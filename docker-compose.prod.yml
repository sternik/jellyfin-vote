services:
  jellyfin-vote:
    image: ghcr.io/sternik/jellyfin-vote:latest
    container_name: jellyfin-vote
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped